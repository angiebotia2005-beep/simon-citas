info:
  name: Consultar Citas ID Paciente
  type: http
  seq: 1

http:
  method: GET
  url: http://localhost:8080/api/citas/paciente/1
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
